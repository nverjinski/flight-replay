# Flight Replay

Telemetry debrief platform demo.

## Layout

- `backend/` — Python package (FastAPI later)
- `frontend/` — React app (later)
- `data/raw/` — original telemetry JSONL
- `data/normalized/` — normalized output
- `docs/` — schema and design notes

## Status

Phase 0 in progress. See [ROADMAP.md](ROADMAP.md).